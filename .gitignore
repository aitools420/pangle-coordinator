# ── Secrets / state — NEVER commit (this service holds a minter key) ──
.env
.env.*
.env.bak*
*.env
backups/
*.db
*.db-*
data/*.db
data/*.db-*
*.key
*.pem
secrets/

# ── Build / deps ──
node_modules/
dist/
contracts/out/
contracts/cache/
contracts/lib/
contracts/broadcast/
*.log
